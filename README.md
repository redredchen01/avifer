# AVIF 批量轉換工具

一個高效的本地批量AVIF圖片轉換工具，支持多種圖片格式導入並批量轉換為AVIF格式。

## 功能特點

- 🚀 **高性能**: 使用Sharp庫進行快速圖片處理
- 🔄 **批量處理**: 支持目錄批量轉換，保持目錄結構
- ⚡ **並發轉換**: 多線程並發處理，提升轉換速度
- 📊 **進度顯示**: 實時顯示轉換進度和統計信息
- 🎛️ **參數控制**: 支持質量、速度等參數調整
- 💻 **跨平台**: 支持Windows、macOS、Linux

## 安裝

```bash
# 克隆項目
git clone <repository-url>
cd avif-bulk-converter

# 安裝依賴
npm install

# 全局安裝
npm install -g .
```

## 使用方法

### 單文件轉換

```bash
avif-converter convert input.jpg output.avif --quality 80 --speed 6
```

### 批量轉換

```bash
avif-converter batch ./input-dir ./output-dir --quality 80 --concurrent 4
```

### 查看支持格式

```bash
avif-converter formats
```

## 參數說明

- `--quality`: 壓縮質量 (1-100，默認80)
- `--speed`: 編碼速度 (1-10，默認6)
- `--pattern`: 文件匹配模式 (默認: `**/*.{jpg,jpeg,png,webp,gif}`)
- `--concurrent`: 並發處理數量 (默認4)

## 支持格式

### 輸入格式
- JPEG/JPG
- PNG
- WebP
- GIF
- SVG
- TIFF
- BMP

### 輸出格式
- AVIF

## 開發

```bash
# 開發模式
npm run dev

# 運行測試
npm test

# 打包可執行文件
npm run build
```

## 系統要求

- Node.js 18+
- 支持的作業系統：Windows 10+、macOS 11+、Linux

## 許可證

MIT License